# 资源说明

将你的素材放到本目录：

- `background.png`：1920x1080 背景图（固定画面主视觉）
- `avatar_a.png`：Speaker A 头像（建议正方形，脚本会裁成圆形）
- `avatar_b.png`：Speaker B 头像（建议正方形，脚本会裁成圆形）
- `bgm.mp3`（可选）：背景音乐，可通过 `--bgm assets/bgm.mp3` 传入

如果未提供 `background.png` / 头像，`run.sh` 会自动生成纯色占位图，保证最小可跑通。
